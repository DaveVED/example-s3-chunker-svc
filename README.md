# fundrick chunker service

```bash
    curl localhost:3000/api/v1/chunker/health

curl -X POST      localhost:3000/api/v1/chunker/upload      -H 'Content-Type: multipart/form-data'      -F 'file=@test/files/README.md'      -F 'user=daveved'      -F 'filePath=.'


```
